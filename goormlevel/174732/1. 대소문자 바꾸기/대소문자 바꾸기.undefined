#include <stdio.h>
int main() {
	int string_length;
	char input[10001];
	scanf("%d\n", &string_length);
	scanf("%s\n", input);
	for (int i = 0; i < string_length; i++) {
		if (input[i] >= 97)
			input[i] = input[i] - 32;
		else
			input[i] = input[i] + 32;
	}

	printf("%s", input);

	return 0;
}
